apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments-mongo-depl
spec:
  template:
    spec:
      volumes:
        - name: mongo-data
          emptyDir: {}
      containers:
        - name: payments-mongo
          volumeMounts:
            - name: mongo-data
              mountPath: /data/db
